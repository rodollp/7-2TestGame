# DevLog - 2026-07-24

## 오늘 작업

### 1. GameStateManager 리팩토링

#### CameraRotate 제어 추가
- CameraRotate를 GameStateManager에서 관리하도록 변경
- 게임 상태에 따라 카메라 회전 가능 여부를 제어

상태별 동작

| State | Camera Rotate |
|-------|---------------|
| Title | ❌ |
| Playing | ✅ |
| LevelUp | ❌ |
| GameOver | ❌ |
| GameClear | ❌ |

추가 메서드

```csharp
private void SetCameraRotate(bool canRotate)
```

---

### 2. CameraRotate 개선

카메라 회전 여부를 외부에서 제어할 수 있도록 수정

추가

```csharp
public void SetRotate(bool value)
```

Update에서

```csharp
if (!canRotate)
    return;
```

를 추가하여 상태에 따라 입력을 차단하도록 변경.

---

### 3. 경험치 오브(ExpOrb) 개선

기존 이동 방식

```csharp
transform.position += dir.normalized * speed * Time.deltaTime;
```

변경

```csharp
transform.position = Vector3.MoveTowards(...)
```

변경 이유

- 목표 위치를 지나치는 현상 방지
- 보다 안정적인 추적
- 향후 Object Pool 적용 시에도 안정적으로 동작

---

### 4. 경험치 오브 설계 방향 결정

게임 디자인을 다시 검토.

초기에는 빠르게 플레이어에게 흡수되는 방식을 고려했지만,

참고 게임(BALL x PIT)처럼

- 플레이어가 경험치 획득 타이밍을 조절할 수 있는 플레이

를 목표로 결정.

현재 규칙

- CollectionRange 안으로 들어오면 추적 시작
- 한 번 추적을 시작하면 계속 플레이어를 따라감
- 플레이어가 움직이면 획득을 늦출 수 있음
- 플레이어가 멈추거나 접근하면 경험치 획득

---

## 리팩토링

### 완료

- CameraRotate 상태 제어
- ExpOrb 이동 방식 개선

### 예정

- MonsterSpawner Spawn 위치 재시도
- Object Pool 적용
- ExpOrb / GoldOrb Pool 적용

---

## 느낀 점

오늘은 기능을 추가하기보다
게임의 흐름과 책임을 정리하는 작업을 진행했다.

특히 GameStateManager가
게임 상태에 따라 카메라 입력을 제어하도록 변경하면서

"상태를 결정하는 객체"와
"실제로 동작하는 객체"의 역할을 구분하는 설계를 적용할 수 있었다.

또한 경험치 오브의 동작을 단순히 빠르게 흡수하는 방식이 아니라

플레이어가 레벨업 타이밍을 조절할 수 있는
게임 플레이 요소로 활용하기로 방향을 확정하였다.